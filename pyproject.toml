[project]
name = "vibecodelang"
version = "0.1.0"
description = "VibeCodeLang interpreter and CLI"
requires-python = ">=3.10"

[project.scripts]
vibecoded = "vibecodelang.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
